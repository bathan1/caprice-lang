# SAT Solving
The Boolean Satisfiability problem was the first problem shown to be NP-complete. Skipping a ton of boolean algebra,
one way to ask the problem is:

> Given some propositional formula with variables `p`, `q`, and `r`, can 

$$
(p) \land (\neg p \lor q) \land (\neg q)
$$
