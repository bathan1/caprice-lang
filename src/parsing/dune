(ocamllex lexer)

(menhir
 (modules tokens)
 (flags --only-tokens)
 (explain false))

(menhir
 (modules tokens parser)
 (merge_into parser)
 (flags --external-tokens Tokens))

(library
 (name parsing)
 (public_name caprice.parsing)
 (synopsis "Caprice parser")
 ; Turn off warning about unused functor parameter in the autogenerated parser mli because
 ;   the functor parameter does not affect types and will not show up in the mli.
 (flags (:standard -w -67))
 (libraries
  ; internal
  lang
  ; external
  cmdliner
 )
 (instrumentation (backend landmarks --auto))
)
