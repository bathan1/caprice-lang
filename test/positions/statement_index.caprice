(* TEST
  statementindexcheck;
  # check index = max(0, change_index - 1), so we also check the stmt before the edit
  # add char before stmt 0,
  # add char inside stmt 1,
  # finish stmt 2,
  # paste a line before stmt 2,
  # delete full stmt 1 line span,
  # paste beyond EOF
  changes = "14:1-14:1, 15:5-15:6, 16:15-16:15, 16:1-16:1, 15:1-15:16, 17:1-17:1";
  statement_indexes = "0, 0, 1, 1, 0, -1";
*)

let a : int = 1
let b : int = 2
let c : int = 3