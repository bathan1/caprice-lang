(* TEST
  include finite-well-typed;
  positions = "6:1-6:39, 8:1-8:38, 10:1-23:26";
*)

let _ : bool = false && (assert false)

let _ : bool = true || (assert false)

let f (a : bool) (b : bool) (c : bool) : { b : bool | b } =
  if a || b then
    if a then
      a
    else
      b
  else
    if a && b then
      if c then
        a
      else
        b
    else
      a || b || c || true
